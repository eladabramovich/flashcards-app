import Container from '../../components/UI/Container/Container';

import styles from './QuizPage.module.css';

const QuizPage = () => {
  return (
    <main className={styles.quizPage}>
      <Container>Quiz App</Container>
    </main>
  );
};

export default QuizPage;
